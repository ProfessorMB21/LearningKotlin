fun main() {
    // declaring an empty array
    val emptyArr = emptyArray<String>()

    val name = "Bupe Mwansa"
    // declaring a string array
    val stringArr = arrayOf(name)

    val integers = arrayOf(1, 2, 3, 4, 5, 6, 7, 8, 9)

    println(emptyArr.size)
    println(stringArr.size)
    println(integers.size)
}